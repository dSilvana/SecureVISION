int main(void)
{
	 FILE *fp;

	    char buffer[128]= {0};

	    f_mount(fp, "D:\\best_int8_dynamic.onnx",0);

	    f_open(fp, "D:\\best_int8_dynamic.onnx", "r");

	    f_read(*fp, *buffer, 128, 128);

	    Xil_Out32(0x41200000, *buffer);

	return 0;
}
